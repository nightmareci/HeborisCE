#pragma once

#define PROJECT_VER "@PROJECT_VER@"
#define PROJECT_ORG "@PROJECT_ORG@"
#define PROJECT_APP "@PROJECT_APP@"

#define FILESYSTEM_WORKINGDIR  0
#define FILESYSTEM_PORTABLE    1
#define FILESYSTEM_INSTALLABLE 2
#define FILESYSTEM_PHYSFS      3
#define FILESYSTEM_TYPE        @FILESYSTEM_TYPE@

#define BASE_PATH_APPEND "@BASE_PATH_APPEND@"

#cmakedefine ENABLE_LINUX_GPIO
#cmakedefine ENABLE_KEYBOARD
#cmakedefine ENABLE_GAME_CONTROLLER
#cmakedefine ENABLE_JOYSTICK

#cmakedefine ONLY_CONTROLLER_TYPE @ONLY_CONTROLLER_TYPE@
#cmakedefine ONLY_SDL_CONTROLLER_TYPE @ONLY_SDL_CONTROLLER_TYPE@

#define DEFAULT_JOYKEY_ASSIGN @DEFAULT_JOYKEY_ASSIGN@
#define DEFAULT_SCREEN_MODE @DEFAULT_SCREEN_MODE@

#define SCREEN_SUBPIXEL_OFFSET @SCREEN_SUBPIXEL_OFFSET@

#define FRAMEWORK_VER "@FRAMEWORK_VER@"

#if SDL_BYTEORDER == SDL_LIL_ENDIAN
#define SWAP16(X)    (X)
#define SWAP32(X)    (X)
#else
#define SWAP16(X)    SDL_Swap16(X)
#define SWAP32(X)    SDL_Swap32(X)
#endif

#cmakedefine ALL_VIDEO_SETTINGS

#cmakedefine ENABLE_GAME_QUIT
